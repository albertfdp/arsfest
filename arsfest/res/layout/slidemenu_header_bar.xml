<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/layoutSlideMenuHeader"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical" >
<!--
    <ImageView
        android:id="@+id/imageViewLogoSlideBar"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:contentDescription="@string/DTU"
        android:src="@drawable/icon" />

    <TextView
        android:id="@+id/textViewDateSlideBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/imageViewLogoSlideBar"
        android:layout_marginLeft="8dip"
        android:layout_toRightOf="@+id/imageViewLogoSlideBar"
        android:text="@string/date"
        android:textSize="15sp"
        android:textColor="@color/flat_white" />

    <TextView
        android:id="@+id/textViewNameSlideBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/textViewDateSlideBar"
        android:layout_alignLeft="@+id/textViewDateSlideBar"
        android:text="@string/app_name"
        android:textSize="22sp"
        android:textColor="@color/flat_white" />
-->
</RelativeLayout>